[sound_system]
music = {"type": "solo"} # solo = only one at a time
effects = {"type": "simultaneous", "simultaneous_sounds": 3, "default": true} # Simul = play all at once
voice = {"type": "sequential"} # Sequential = queue